# social-network

Install client:

cd app
composer install

Install and launch server

cd server
npm install
node index.js

Define domain
sudo nano /etc/hosts
add line : your.local.ip.adress social.network
